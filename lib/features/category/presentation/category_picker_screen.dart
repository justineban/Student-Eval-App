import 'package:flutter/material.dart';

class CategoryPickerScreen extends StatelessWidget {
  const CategoryPickerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Seleccionar Categoría')), body: const Center(child: Text('Category picker (placeholder)')));
  }
}
