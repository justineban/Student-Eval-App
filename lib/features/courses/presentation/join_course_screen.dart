import 'package:flutter/material.dart';

class JoinCourseScreen extends StatelessWidget {
  const JoinCourseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Unirse a curso')), body: const Center(child: Text('Join course (placeholder)')));
  }
}
